@model Product
@{
    ViewData["Title"] = "Create product item";
}
<h1>@ViewData["Title"]</h1>

<p>This page creates product item.</p>

@{
    ViewBag.Action = nameof(ProductController.Create);
}
<partial name="CreateEditForm" model="Model">